import React from 'react';
import Cardbox from './CardBox';

const Discardbox = React.createClass({
    renderCard() {
	
    },
    render() {
	return(
		<div>
		
		</div>
	);
    },
    propTypes: {
	card: React.PropTypes.object
    }
});
